<ul class="nav navbar-nav">
    <li class="dropdown">
        <%= link_to dashboard_path, class: "navbar-brand dropdown-toggle", :data => {:toggle=>"dropdown"} do %>
            <%= @team.name %><span class="caret"></span>
        <%end%>
        <ul class="dropdown-menu" role="menu">
            <% @other_teams.each do |t| %>
            <li><%=link_to t.name, team_path(t) %></li>
            <%end%>
            <li class="divider"></li>
            <li><%= link_to "Edit team settings", edit_team_path(@team), remote: true%></li>
            <li><%= link_to "New Team", new_team_path, remote: true%></li>
        </ul>
    </li>
</ul>